<div id="left">  
    <div id="participants">
        <span style='font-size:130%'><%= link_to  h(@post.subject.capitalize), @post.get_url_for(@user, 'show'), :class => "#{@post.unread_comments_for(@user) > 0 ? "unread-post" : "read-post"}"%></span><br/>
        <%= @contest.matches.size %> matches
        <h3 style="border-top:1px solid #fff;"> </h3>
        <%= render :partial => 'posts/match', :collection => @contest.matches %>
    </div>
</div>

